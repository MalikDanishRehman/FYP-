@page "/VendorManagement"
@using AI_Driven_Water_Supply.Application.Interfaces
@using AI_Driven_Water_Supply.Domain.Entities
@layout AdminSidebar

<div class="page-header">
    <div class="header-title">
        <h2>Vendor <span>Management</span></h2>
        <small style="color: #666;">Manage your water suppliers and their status</small>
    </div>

    <div class="search-wrapper">
        <i class="fa-solid fa-search search-icon"></i>
        <input type="text" class="custom-input" placeholder="Search vendor by name...">
    </div>
</div>

<div class="table-card">
    <table class="vendor-table">
        <thead>
            <tr>
                <th>Vendor Profile</th>
                <th>Shop Location</th>
                <th>Performance</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @if (IsLoading)
            {
                <tr>
                    <td colspan="5" style="text-align: center; padding: 20px;">
                        <span style="color: #BFFF27;">Loading vendors from database...</span>
                    </td>
                </tr>
            }
            else if (!Vendors.Any())
            {
                <tr>
                    <td colspan="5" style="text-align: center; padding: 20px;">
                        No vendors (Providers) found.
                    </td>
                </tr>
            }
            else
            {
                @foreach (var vendor in Vendors)
                {
                    <tr>
                        <td>
                            <div class="vendor-profile">
                                <div class="avatar-box" style="background: linear-gradient(135deg, #3498db, #2980b9);">
                                    @(string.IsNullOrEmpty(vendor.Username) ? "V" : vendor.Username.Substring(0, 1).ToUpper())
                                </div>
                                <div class="vendor-details">
                                    <h4>@(string.IsNullOrEmpty(vendor.Username) ? "Unknown" : vendor.Username)</h4>
                                    <p style="font-size: 0.8rem; color: #888;">ID: @vendor.Id.Substring(0, 8)...</p>
                                </div>
                            </div>
                        </td>
                        <td>
                            <span style="display:block; color:#eee;">@(string.IsNullOrEmpty(vendor.Address) ? "Location not updated" : vendor.Address)</span>
                        </td>
                        <td>
                            <div class="rating-box">
                                <i class="fa-solid fa-star" style="color: gold;"></i>
                                <span class="rating-text">(@(vendor.Rating ?? 0.0f))</span>
                            </div>
                        </td>
                        <td><span class="status-badge badge-verified">Active</span></td>
                        <td>
                            <div class="action-group">
                                <button class="btn-icon" title="View Profile"><i class="fa-regular fa-eye"></i></button>
                                <button class="btn-icon btn-ban" title="Ban Vendor"><i class="fa-solid fa-ban"></i></button>
                            </div>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>